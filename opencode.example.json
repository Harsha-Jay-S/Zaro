{
  "$schema": "https://opencode.ai/config.json",
  "_comment": "Add ./plugins/zaro.js to your opencode `plugin` array. If you already register other plugins (e.g. caveman, superpowers), append this one — do not replace them.",
  "plugin": [
    "./plugins/zaro.js"
  ]
}
